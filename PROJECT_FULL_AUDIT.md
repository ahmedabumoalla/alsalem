# التدقيق الكامل لمشروع FoamSales

> تاريخ التدقيق: 2026-07-21  
> نطاق التدقيق: جميع ملفات المشروع الفعلية مع استبعاد `node_modules` و`.next` و`dist` و`build` و`.git`  
> المنهج: قراءة المصدر والإعدادات والتوثيق، تتبع التدفقات من الواجهة إلى التخزين، فحص Git، وتشغيل الفحوص الآمنة على المصدر الحالي  
> التعديل الوحيد الذي أجراه هذا التدقيق داخل المشروع: إنشاء هذا التقرير

## 1. ملخص تنفيذي

**FoamSales | مبيعات الفلين** تطبيق ويب عربي باتجاه RTL لإدارة فواتير مبيعات الفلين على جهاز واحد. ينفذ إنشاء الفاتورة وتعديلها وحذفها، حساب المبيعات والتكلفة والربح والتحصيل، رفع صورة حوالة مضغوطة، وعرض تقارير وفلاتر ورسوم بيانية وتصدير CSV. الدليل الأساسي: `src/app/layout.tsx`، `src/components/sales/invoice-form.tsx`، `src/app/reports/page.tsx`، و`src/lib/storage/invoice-storage.ts`.

المشروع **ليس منصة أعمال متعددة المستخدمين بعد**. لا يوجد backend أو قاعدة بيانات أو API أو مصادقة أو أدوار أو مزامنة أو نسخ احتياطي. كل الفواتير وصور الحوالات محفوظة في سجل `localStorage` واحد داخل المتصفح تحت المفتاح `foam_sales_invoices` (`src/lib/types/invoice.ts:39`). لذلك فالمنتج الحالي أنسب لوصف **MVP محلي/نموذج عملي متوسط الاكتمال**، وليس نظامًا جاهزًا لإطلاق إنتاجي يتعامل مع بيانات مالية فعلية.

أهم النتائج:

| النتيجة | الحكم | الدليل |
|---|---|---|
| الوظيفة الأساسية للفواتير والتقارير | موجودة وتعمل محليًا | `src/components/sales/invoice-form.tsx`، `src/lib/storage/invoice-storage.ts`، `src/app/reports/page.tsx` |
| البيانات الحقيقية | حقيقية داخل المتصفح فقط؛ ليست mock بعد إدخال المستخدم | `src/lib/hooks/use-invoices.ts`، `src/lib/storage/invoice-storage.ts` |
| الصفحة الرئيسية | الإحصائيات حقيقية؛ بطاقة معاينة الفاتورة وهمية وموسومة كمثال | `src/components/home/stats-overview.tsx`، `src/components/home/hero-section.tsx:26-82` |
| المصادقة والأدوار | غير موجودة | لا ملفات auth أو session أو Proxy/Middleware؛ كل المسارات مفتوحة |
| API وقاعدة البيانات | غير موجودتين | صفر `route.ts`، لا migrations/schema/SQL، ولا عميل قاعدة بيانات |
| الدفع | تسجيل حالة وطريقة ومبالغ فقط؛ لا تحصيل إلكتروني | `src/lib/utils/invoice-calculations.ts:42-55` |
| متابعة التسليم | تاريخ تسليم فقط؛ لا حالة أو رحلة تسليم | `src/lib/types/invoice.ts:11`، `src/components/sales/invoice-form.tsx:398-405` |
| البناء والجودة الساكنة | `tsc` وlint والبناء ناجحة | قسم نتائج الفحوص |
| الجاهزية للإطلاق | **32/100** | غياب الهوية والصلاحيات والخادم والنسخ الاحتياطي والاختبارات الشاملة |

## 2. وصف المنصة ونموذج العمل

### الهوية والغرض

- الاسم البرمجي: `foam-sales` (`package.json`).
- الاسم الظاهر: **FoamSales | مبيعات الفلين** (`src/app/layout.tsx:14-17`).
- الغرض: تسجيل فواتير مبيعات الفلين، حساب الربح، متابعة المبالغ المحصلة والمستحقة، وعرض تقارير تشغيلية (`src/components/home/hero-section.tsx:13-25`).
- المستخدم المستهدف الظاهر من الواجهة: مالك نشاط صغير أو موظف مبيعات يسجل الفواتير ويراقب أداء البائعين والعملاء والتحصيل.
- المشكلة التي يحلها: استبدال الحساب اليدوي وسجل الفواتير المتفرق بواجهة موحدة سريعة على متصفح واحد.

### نموذج العمل الظاهر من الكود

لا يوجد نموذج اشتراك أو متجر أو marketplace أو عمولات أو دفع رقمي. النموذج الحالي أداة تشغيل داخلية:

1. إدخال فاتورة مبيعات.
2. حساب الإجمالي والتكلفة والربح فورًا.
3. تسجيل حالة السداد وصورة الحوالة عند الحاجة.
4. حفظ السجل محليًا.
5. تحليل الفواتير وتصديرها.

لا توجد كيانات مستقلة للبائعين أو العملاء أو المنتجات؛ أسماؤهم ومواصفات المنتج محفوظة داخل كل فاتورة كنصوص وأرقام (`src/lib/types/invoice.ts`).

### مرحلة المشروع

**التصنيف: متوسط — MVP محلي قابل للعرض والاستخدام الفردي، غير جاهز للإطلاق التجاري.**

الأسباب:

- الواجهة الأساسية مترابطة والفحوص الساكنة والبناء تنجح.
- CRUD والتقارير تعمل على نفس مصدر البيانات الحقيقي المحلي.
- البنية تفتقد الطبقات اللازمة للإنتاج: حسابات، أدوار، خادم، قاعدة بيانات، نسخ احتياطي، مراقبة، سجلات تدقيق، واختبارات E2E.

## 3. التقنيات والبنية

### الحزم والإصدارات المثبتة

| التقنية | الإصدار المثبت | الاستخدام |
|---|---:|---|
| Next.js | 16.2.6 | App Router، الصفحات، البناء، الخطوط |
| React / React DOM | 19.2.4 | الواجهة وحالة العميل |
| TypeScript | 5.9.3 | الأنواع والفحص الصارم |
| Tailwind CSS | 4.3.0 | CSS utilities والثيم |
| ESLint | 9.39.4 | جودة المصدر |
| eslint-config-next | 16.2.6 | قواعد Next وReact |
| Recharts | 3.8.1 | الرسوم البيانية |
| date-fns | 4.4.0 | التواريخ واللغة العربية |
| Lucide React | 1.17.0 | الأيقونات |
| clsx | 2.1.1 | تركيب classes |
| tailwind-merge | 3.6.0 | حل تعارض classes |

المصدر: `package.json` ونتيجة `npm.cmd ls --depth=0`.

### نمط التطبيق

- App Router داخل `src/app` وفق بنية Next.js 16.
- الصفحة الرئيسية وRoot Layout مكونات خادم افتراضيًا.
- 19 ملفًا يبدأ بـ`"use client"` لأن التخزين والنماذج والرسوم والتفاعل كلها في المتصفح.
- لا توجد Server Actions أو Route Handlers أو Proxy/Middleware.
- `tsconfig.json` يفعّل `strict: true` و`noEmit: true` و`moduleResolution: "bundler"` ومسار `@/*`.
- `next.config.ts` بلا إعدادات فعلية؛ لا headers أو redirects أو rewrites أو output mode.

### التصميم وCSS والخطوط

- Tailwind CSS 4 عبر `@tailwindcss/postcss` (`postcss.config.mjs`).
- متغيرات ألوان خاصة في `src/app/globals.css:3-23`.
- خط Tajawal بأوزان 400/500/700/800 من `next/font/google` (`src/app/layout.tsx:2-11`).
- fallback خطي إلى Arial (`src/app/globals.css:25-29`).
- دعم RTL و`lang="ar"` على عنصر HTML الجذري (`src/app/layout.tsx:23-27`).

### قاعدة البيانات والتخزين والمصادقة والنشر

| الجانب | الموجود فعليًا |
|---|---|
| قاعدة البيانات | لا توجد |
| التخزين الدائم | `window.localStorage` فقط |
| تخزين الملفات | Data URL WebP داخل JSON نفسه |
| المصادقة | لا توجد |
| الجلسات | لا توجد |
| الاستضافة | غير محددة؛ README قالب Create Next App يذكر Vercel فقط |
| متغيرات البيئة | لا ملفات `.env` ولا استخدام `process.env` في المصدر |
| APIs خارجية runtime | لا توجد |
| اعتماد خارجي وقت البناء | Google Fonts لجلب Tajawal عبر `next/font/google` |

## 4. خريطة المجلدات

```text
E:\foam-sales
├── docs/
│   └── FOAMSALES_HANDOFF_REPORT.md   توثيق تسليم سابق، وبعض أقسامه قديمة
├── public/                            أصول Create Next App الافتراضية
├── scripts/
│   ├── verify-logic.mjs              فحص يدوي لخمسة نواتج حسابية
│   └── verify-snapshot-cache.mjs     فحص مرجع snapshot محاكى
├── src/
│   ├── app/
│   │   ├── layout.tsx                RTL والخط والرأس وToastProvider
│   │   ├── page.tsx                  الرئيسية
│   │   ├── reports/page.tsx          لوحة التقارير
│   │   └── sales/
│   │       ├── new/page.tsx          إنشاء فاتورة
│   │       └── [id]/edit/page.tsx    تعديل فاتورة ديناميكية
│   ├── components/
│   │   ├── home/                     Hero وإحصائيات وبطاقات مزايا
│   │   ├── layout/                   Header وPageContainer
│   │   ├── reports/                  KPIs وفلاتر ورسوم وجدول وmodals
│   │   ├── sales/                    نموذج الفاتورة والملخص ورفع الحوالة
│   │   └── ui/                       Button/Card/Input/Select/Modal/Toast
│   └── lib/
│       ├── hooks/                    اشتراك React في بيانات الفواتير
│       ├── storage/                  CRUD وpub/sub داخل المتصفح
│       ├── types/                    نموذج Invoice
│       └── utils/                    حسابات وتصفية وتنسيق وصور وCSV
├── package.json                       الحزم والأوامر
├── next.config.ts                     إعداد Next فارغ
├── tsconfig.json                      TypeScript strict/noEmit
├── eslint.config.mjs                  ESLint Next Core Web Vitals
└── postcss.config.mjs                 Tailwind PostCSS
```

ملاحظات:

- `public/` يحتوي شعارات `next.svg` و`vercel.svg` و`globe.svg` و`file.svg` و`window.svg` الافتراضية، ولم يظهر لها استخدام في المصدر.
- `src/app/favicon.ico` يبدو أصل Create Next App ولم توجد أيقونات علامة تجارية أو OG أو Apple.
- أكبر ملفات المصدر: `invoice-form.tsx` (638 سطرًا)، `sales-charts.tsx` (275)، `invoices-table.tsx` (236)، و`invoice-details-modal.tsx` (179).

## 5. جدول كامل للمسارات والصفحات

### صفحات المنتج

| المسار | الوظيفة | الوصول | الحالة | أهم الملفات |
|---|---|---|---|---|
| `/` | صفحة ترحيب، CTA، 4 إحصائيات حقيقية، و3 بطاقات مزايا | عام بلا مصادقة | مكتملة كواجهة؛ المعاينة داخل Hero وهمية وموسومة | `src/app/page.tsx`، `src/components/home/*` |
| `/sales/new` | إنشاء فاتورة وحسابها ورفع حوالة وحفظها محليًا | عام بلا مصادقة | مكتملة محليًا؛ لا خادم ولا تدقيق | `src/app/sales/new/page.tsx`، `src/components/sales/invoice-form.tsx` |
| `/reports` | KPIs وفلاتر ورسوم وجدول وعرض/تعديل/حذف وCSV | عام بلا مصادقة | مكتملة محليًا مع فجوات أمان واختبار | `src/app/reports/page.tsx`، `src/components/reports/*` |
| `/sales/[id]/edit` | جلب فاتورة من localStorage بالـID وتعديلها | عام بلا مصادقة | مكتملة محليًا؛ الفقد يعرض EmptyState داخل HTTP 200 | `src/app/sales/[id]/edit/page.tsx`، `src/components/sales/invoice-form.tsx` |

**عدد صفحات/مسارات المنتج: 4، منها مسار ديناميكي واحد.**

أظهر بناء Next كذلك `/_not-found` كصفحة framework مولدة، لكن لا يوجد `not-found.tsx` مخصص في المشروع.

### مسارات API والملفات الخاصة

| النوع | العدد | الملاحظة |
|---|---:|---|
| Route Handlers (`route.ts`) | 0 | لا API داخلية |
| Server Actions (`"use server"`) | 0 | لا mutations على الخادم |
| Proxy/Middleware | 0 | لا حماية أو redirects على مستوى الطلب |
| `loading.tsx` | 0 | توجد مؤشرات تحميل محلية في صفحتي الإنشاء والتعديل فقط |
| `error.tsx` / `global-error.tsx` | 0 | الاعتماد على أخطاء Next الافتراضية |
| `not-found.tsx` | 0 | لا 404 مخصص |
| `manifest.*` | 0 | لا PWA manifest |
| `sitemap.*` / `robots.*` | 0 | غير موجودة |

## 6. الأدوار والصلاحيات

### الأدوار الفعلية

لا يوجد تعريف رسمي لأي دور. الدور الفعلي الوحيد هو:

| الدور الفعلي | التحديد | الصلاحيات |
|---|---|---|
| زائر/مستخدم المتصفح المحلي غير الموثق | مجرد فتح أصل الموقع | قراءة كل الفواتير المحلية، إنشاء، تعديل، حذف، عرض التكلفة والربح وصور الحوالات، وتصدير CSV |

**عدد الأدوار الرسمية: 0. عدد أنماط الوصول الفعلية: 1 غير مقيّد.**

### آلية الحماية

- حماية الصفحات: غير موجودة.
- حماية API: لا APIs أصلًا.
- تحديد الدور: غير موجود.
- RLS: غير موجودة لعدم وجود قاعدة بيانات.
- Impersonation: غير موجود.
- Maintenance mode: غير موجود.
- سجل تدقيق للعمليات الحساسة: غير موجود.

النتيجة: أي شخص يصل إلى المتصفح/الملف الشخصي نفسه يستطيع مشاهدة البيانات المالية وتغييرها أو حذفها. الفصل بين البائع والمدير والمحاسب غير مطبق، حتى لو كانت أسماء البائعين تظهر في الفواتير.

## 7. المزايا حسب الدور/القسم

### المستخدم المحلي غير الموثق

| الميزة | ماذا تفعل | البيانات | الحالة | الملفات الرئيسية |
|---|---|---|---|---|
| إنشاء فاتورة | يجمع البيانات ويحسب القيم ويحفظ JSON | حقيقية محلية | مكتملة محليًا | `invoice-form.tsx`، `invoice-storage.ts` |
| رقم فاتورة تلقائي | `FS-YYYYMMDD-XXXX` مع فحص تصادم محلي | حقيقية محلية | مكتملة جزئيًا؛ لا قيد مركزي | `invoice-number.ts` |
| حساب المبيعات والربح | subtotal/total/cost/profit/margin | حقيقية مشتقة | مكتملة | `invoice-calculations.ts` |
| حالات السداد | مدفوع/جزئي/آجل ومبالغ مدفوعة/متبقية | حقيقية مدخلة/مشتقة | مكتملة كسجل فقط | `invoice-form.tsx`، `invoice-calculations.ts` |
| رفع إثبات حوالة | يتحقق من MIME ثم يصغر ويحوّل إلى WebP ≤700KB | حقيقية محلية | مكتملة محليًا | `receipt-uploader.tsx`، `image-compression.ts` |
| تعديل فاتورة | يعيد تعبئة النموذج ويستبدل السجل | حقيقية محلية | مكتملة | `[id]/edit/page.tsx`، `invoice-storage.ts` |
| حذف فاتورة | حذف بعد modal تأكيد | حقيقية محلية | مكتملة لكن غير قابلة للاسترجاع | `invoices-table.tsx:30-39`، `invoice-storage.ts:99-104` |
| عرض التفاصيل | Modal شامل وصورة الحوالة عند وجودها | حقيقية محلية | مكتملة | `invoice-details-modal.tsx` |
| إحصائيات الرئيسية | عدد/مبيعات/أرباح/كمية | حقيقية محلية | مكتملة | `stats-overview.tsx` |
| KPIs التقارير | 8 مؤشرات بعد الفلترة | حقيقية محلية | مكتملة | `report-stat-cards.tsx`، `invoice-filters.ts` |
| الفلاتر | تاريخ، بائع، عميل، حالة، طريقة، ضغط | حقيقية محلية | مكتملة | `report-filters.tsx`، `invoice-filters.ts` |
| الرسوم | المبيعات/الأرباح زمنيًا، حالات/طرق السداد، أرباح البائع | حقيقية محلية | مكتملة كعرض | `sales-charts.tsx` |
| ملخص التحصيل | عدد غير المسدد، المتبقي، أعلى فاتورة | حقيقية محلية | مكتملة كملخص فقط | `calculateCollectionSummary` في `invoice-filters.ts` |
| CSV | يصدر النتائج المفلترة بـ22 عمودًا وUTF-8 BOM | حقيقية محلية | موجودة مع خطر Formula Injection | `csv-export.ts` |
| ترحيل الشكل القديم | يحول `credit` القديم إلى `deferred` عند القراءة | بيانات مخزنة سابقة | مكتملة جزئيًا؛ دون schema validation | `invoice-normalize.ts` |
| Toast/Empty/Loading | تغذية راجعة وحالات فراغ ومؤشرات نبض | واجهة | مكتملة جزئيًا | `components/ui/*` وصفحات sales |

### ميزات ليست كما قد يوحي الاسم

- **متابعة التسليمات:** لا توجد حالة `pending/shipped/delivered/cancelled`؛ يوجد `deliveryDate` فقط.
- **الدفع:** لا بوابة دفع ولا معاملات ولا refunds؛ التطبيق يسجل حالة ومبلغًا يدوياً.
- **إدارة العملاء والبائعين:** لا صفحات أو جداول مستقلة؛ مجرد قيم نصية متكررة داخل الفواتير.
- **لوحة التحكم:** الرئيسية والتقارير تعملان كلوحتي عرض للمستخدم الوحيد، وليستا لوحة إدارة بصلاحيات.

## 8. رحلات المستخدم الرئيسية

### 8.1 إنشاء فاتورة

1. الانتقال من `/` أو `/reports` إلى `/sales/new`.
2. انتظار hydration قصير عبر `useIsClient()`.
3. توليد رقم محلي عبر `generateInvoiceNumber()`.
4. إدخال التاريخين والبائع والعميل وحالة/طريقة السداد.
5. إدخال الأبعاد والضغط والوزن والأسعار والكمية والتوصيل.
6. إعادة حساب الملخص لحظيًا بواسطة `calculateInvoiceTotals()`.
7. عند تحويل بنكي يمكن ضغط صورة وتحويلها إلى Data URL.
8. `validateForm()` يفحص الحقول الأساسية.
9. إنشاء UUID بواسطة `crypto.randomUUID()`.
10. `createInvoice()` يقرأ المصفوفة ويضيف السجل ويكتب JSON.
11. إطلاق event داخلي وتحديث المشتركين.
12. Toast نجاح ثم انتقال إلى `/reports`.

الصفحات: `/sales/new` ثم `/reports`.  
APIs: لا توجد.  
الجداول: لا توجد؛ مجموعة localStorage منطقية واحدة.  
الحالات: `paid | partial | deferred`.  
النقص: لا transaction خادمي، لا تحقق مركزي، لا سجل تدقيق، لا نسخة احتياطية، ولا منع تنافس بين تبويبات/أجهزة.

### 8.2 التقارير والتحليل

1. `/reports` يقرأ snapshot من localStorage عبر `useSyncExternalStore`.
2. الفلاتر تعمل في الذاكرة على المصفوفة.
3. الإحصائيات والرسوم تُحسب بـ`useMemo`.
4. الجدول يوفر عرضًا وتعديلًا وحذفًا.
5. زر التصدير يولد Blob CSV في المتصفح.

النقص: لا pagination أو server-side aggregation أو نطاق وصول حسب المستخدم؛ الأداء والذاكرة يعتمدان على حجم JSON والمتصفح.

### 8.3 التعديل

1. اختيار تعديل من الجدول.
2. الانتقال إلى `/sales/{id}/edit`.
3. البحث داخل snapshot المحلي بالـID.
4. عند الفقد يظهر EmptyState؛ عند الوجود يعاد استخدام النموذج.
5. الحفظ يستبدل السجل ويحافظ على `createdAt` ويغير `updatedAt`.

النقص: لا optimistic concurrency؛ التعديل الأخير يفوز، ولا تاريخ إصدارات.

### 8.4 الحذف

1. اختيار الحذف من الجدول.
2. عرض Modal تأكيد.
3. `deleteInvoice()` يفلتر السجل ويعيد كتابة المجموعة.
4. Toast نجاح.

النقص: لا soft delete أو سلة محذوفات أو audit log أو صلاحية خاصة أو إمكانية استرجاع.

### 8.5 التحصيل والتسليم

- التحصيل يُحدد فقط عند إنشاء/تعديل الفاتورة، ولا توجد دفعات متعددة أو سجل زمني أو إيصالات مستقلة أو تنبيهات استحقاق.
- التسليم يقتصر على تاريخ مخطط؛ لا يوجد تنفيذ رحلة أو إثبات تسليم أو إلغاء.

## 9. قاعدة البيانات والجداول والعلاقات

### النتيجة

لا توجد قاعدة بيانات ولا migrations ولا schema ولا SQL ولا ORM ولا جداول أو views أو functions أو triggers أو enums على مستوى قاعدة البيانات أو RLS أو سياسات Storage.

**عدد جداول قاعدة البيانات: 0.**

يوجد بديل منطقي واحد داخل المتصفح:

| المخزن المنطقي | الغرض | أهم الحقول | العلاقات | المستهلكون | النقص |
|---|---|---|---|---|---|
| `foam_sales_invoices` | JSON array من الفواتير | id، invoiceNumber، dates، seller/customer، payment، dimensions، prices، totals، receipt، timestamps | لا علاقات؛ كل شيء مضمن | الرئيسية، النموذج، التقارير | بلا قيود DB أو فهارس أو نسخ احتياطي أو مشاركة أو integrity مركزي |

### نموذج `Invoice`

النموذج في `src/lib/types/invoice.ts` يضم 30 حقلًا تقريبًا:

- الهوية: `id`, `invoiceNumber`.
- التواريخ: `invoiceDate`, `deliveryDate`, `createdAt`, `updatedAt`.
- الأطراف: `sellerName`, `customerName`.
- السداد: `paymentStatus`, `paymentMethod?`, `amountPaid`, `amountDue`.
- المواصفات: `lengthCm`, `widthCm`, `thicknessCm`, `densityPressure`, `weightKg?`.
- المال والكمية: `unitSalePrice`, `unitCost`, `quantity`, `deliveryFee`.
- المحتوى: `notes?`, `transferReceipt?`.
- القيم المشتقة المخزنة: `productSubtotal`, `invoiceTotal`, `totalCost`, `netProfit`, `profitMargin`.

لا توجد مفاتيح أجنبية. تكرار البائع والعميل ومخرجات الحساب داخل كل فاتورة يسهل MVP لكنه يسمح بعدم الاتساق ويعقد التقارير والتحديثات المستقبلية.

### التخزين والوصول

- `localStorage` مرتبط بالـorigin وملف المتصفح، وليس بالمستخدم.
- صور الحوالة ضمن السجل نفسه وتستهلك حصة localStorage.
- الكتابة تمسك `QuotaExceededError` وتعرض رسالة عربية (`invoice-storage.ts:57-72`).
- JSON التالف يعرض مصفوفة فارغة بصمت (`invoice-storage.ts:40-46`)؛ لا يمسح القيمة الخام، لكنه يوهم المستخدم بفقد البيانات دون تشخيص أو استرداد.
- نظام pub/sub داخلي لا يستمع إلى حدث `storage` بين التبويبات (`invoice-storage-events.ts`).

## 10. APIs والتكاملات الخارجية

### API داخلية

لا يوجد أي Route Handler أو Server Action أو endpoint. عمليات CRUD دوال متصفح مباشرة:

| العملية | الدالة | الملف |
|---|---|---|
| قراءة الكل | `getInvoicesSnapshot` | `src/lib/storage/invoice-storage.ts` |
| قراءة بالمعرف | `getInvoiceById` / hook مشتق | `invoice-storage.ts` / `use-invoices.ts` |
| إنشاء | `createInvoice` | `invoice-storage.ts` |
| تحديث | `updateInvoice` | `invoice-storage.ts` |
| حذف | `deleteInvoice` | `invoice-storage.ts` |

### التكاملات

| الخدمة/المكتبة | موضع الاستخدام | متغيرات البيئة | الاكتمال | fallback/بيانات وهمية | المخاطر |
|---|---|---|---|---|---|
| Google Fonts عبر Next | Tajawal في `layout.tsx` | لا يوجد | يعمل مع الشبكة أثناء البناء | Arial fallback وقت العرض، لكن build يحتاج fetch إن لم يكن مخزنًا | بناء offline يفشل |
| Recharts | `sales-charts.tsx` | لا يوجد | مكتمل محليًا | EmptyState عند عدم البيانات | كل التجميع على العميل |
| date-fns/arSA | التواريخ والأرقام | لا يوجد | مكتمل | catch يعيد النص الأصلي | لا timezone business rules |
| Web APIs | localStorage/Canvas/Blob/crypto | ملفات storage/utils/components | مكتمل محليًا | رسائل أخطاء محدودة | اختلاف دعم المتصفحات وحصة التخزين |

لا Supabase، Firebase، Stripe، Moyasar، HyperPay، بريد، SMS، WhatsApp، خرائط، analytics، error monitoring، أو object storage.

## 11. لوحة الإدارة

لا توجد لوحة إدارة أو مفهوم مدير. الموجود:

- `/`: إحصائيات عامة محلية للمستخدم نفسه.
- `/reports`: تقارير وفواتير مع كل الإجراءات الحساسة.

لا توجد إدارة مستخدمين أو محتوى أو صلاحيات أو إعدادات أو سجلات تدقيق أو تعطيل حسابات أو عمليات bulk أو approval workflow. كل الأرقام حقيقية من localStorage بعد إدخال المستخدم، باستثناء معاينة Hero الثابتة. لا وظائف إدارية غير متصلة؛ لوحة الإدارة نفسها غير موجودة.

## 12. التصميم وتجربة المستخدم

### نقاط القوة

- هوية ألوان متسقة: أخضر داكن/ثانوي، ذهبي، نجاح، وخطر (`globals.css`).
- RTL وTajawal مطبقان مركزيًا.
- مكونات مشتركة واضحة للأزرار والحقول والبطاقات وToast وModal.
- تخطيطات responsive باستخدام `sm` و`lg`، وجدول بتمرير أفقي.
- حالات فراغ للرسوم والجدول والفاتورة المفقودة.
- ملخص فاتورة sticky على الشاشات الواسعة.
- رسائل تحقق عربية وCTA واضحة.

### المشكلات والنواقص

| المشكلة | الشدة | الدليل/الأثر |
|---|---|---|
| لم يُنفذ تحقق بصري حي في هذا التدقيق | قيد تحقق | تعذر بدء تكامل المتصفح؛ البناء فقط تحقق من render/build |
| Modal بلا focus trap أو Escape أو استعادة focus | متوسطة | `src/components/ui/modal.tsx` |
| Modal التفاصيل يمرر عنوانًا فارغًا مع `aria-labelledby` | متوسطة | `invoice-details-modal.tsx:48-53` + `modal.tsx:40-53` |
| Toast لا يحدد live-region مناسبًا للحاوية، وكل عنصر `role=alert` | منخفضة | `toast.tsx` |
| لا error/loading/not-found مخصص عالميًا | متوسطة | غياب ملفات conventions |
| favicon والأصول العامة تبدو افتراضية | منخفضة | `src/app/favicon.ico` و`public/*.svg` |
| لا dark mode أو تفضيل تباين/حركة | منخفضة | `globals.css` |
| الجدول عريض جدًا (`min-w-[1100px]`) على الجوال | متوسطة | `invoices-table.tsx:61`؛ يعمل بالتمرير لكنه مرهق |
| الرسوم قد تصبح مزدحمة مع تواريخ/بائعين كثيرين | متوسطة | لا aggregation window أو limit في `sales-charts.tsx` |

الاستجابة للجوال موجودة في الكود لكن لم تُثبت باختبار أجهزة/لقطات في هذه الجلسة.

## 13. PWA والجوال

| العنصر | الحالة |
|---|---|
| Web App Manifest | غير موجود |
| Service Worker | غير موجود |
| Install prompt | غير موجود |
| أيقونات 192/512 وApple | غير موجودة |
| Offline caching/sync | غير موجود |
| سياسة تحديث service worker | غير موجودة |
| Responsive CSS | موجود جزئيًا/بوضوح في المكونات |
| تشغيل الوظائف دون شبكة بعد تحميل التطبيق | localStorage محلي، لكن لا ضمان لتحميل assets/routes offline |

المشروع ليس PWA. التخزين المحلي لا يعني دعم offline قابلًا للتثبيت.

## 14. نتائج الفحوص والبناء

| الفحص | الأمر | النتيجة | الأخطاء/التحذيرات | ملفات المشروع المتأثرة |
|---|---|---|---|---|
| TypeScript | `npx.cmd tsc --noEmit --incremental false` | نجاح، exit 0 | لا مخرجات | لا شيء |
| ESLint | `npm.cmd run lint -- --no-cache` | نجاح، exit 0 | لا أخطاء أو تحذيرات | لا شيء |
| منطق الحساب | `node scripts/verify-logic.mjs` | نجاح، 5/5 | لا شيء | لا شيء |
| cache snapshot | `node scripts/verify-snapshot-cache.mjs` | نجاح | لا شيء | لا شيء |
| dependency tree | `npm.cmd ls --depth=0` | نجاح | لا missing/invalid packages | لا شيء |
| build أول في نسخة مؤقتة مقيدة | `npm.cmd run build` | فشل متوقع بيئيًا | تعذر جلب Tajawal من Google Fonts | نسخة مؤقتة محذوفة؛ لا شيء في المشروع |
| build ثان في نسخة مؤقتة مع الشبكة | `npm.cmd run build` | **نجاح، exit 0** | لا تحذيرات نهائية | نسخة مؤقتة محذوفة؛ لا شيء في المشروع |
| npm audit مباشر | `npm.cmd audit --json --omit=optional` | تعذر، exit 1 | `unable to verify the first certificate` | لا شيء في المشروع؛ سجل npm خارجي فقط |
| npm audit غير متصل | `npm.cmd audit --offline --json --omit=optional` | صفر ثغرات في cache المحلي | ليست نتيجة محدثة موثوقة بدل الفحص الحي | لا شيء |

خرج البناء الناجح:

```text
○ /                     static
○ /reports              static
○ /sales/new            static
ƒ /sales/[id]/edit      dynamic
○ /_not-found           generated by Next
```

### حدود الاختبارات

- لا يوجد script باسم `test` في `package.json`.
- السكربتان يدويان ولا يستخدمان test runner أو assertions صارمة متكاملة.
- `verify-logic.mjs` **ينسخ المعادلة** بدل استيراد دالة الإنتاج؛ يمكن أن ينجح حتى لو تغيّر `invoice-calculations.ts` بشكل مختلف.
- `verify-snapshot-cache.mjs` يحاكي المنطق بدل استيراد storage الفعلي.
- لا unit tests للفلاتر والتطبيع والتحقق والصور وCSV.
- لا integration أو E2E أو accessibility أو visual regression.
- لم تُنفذ رحلة متصفح حية في هذا التدقيق بسبب خطأ تهيئة أداة المتصفح؛ التوثيق السابق نفسه يقر أن سيناريوهات المتصفح الكاملة لم تُختبر آليًا (`docs/FOAMSALES_HANDOFF_REPORT.md:567-595`).

## 15. المخاطر الأمنية

لا توجد SQL injection أو CSRF أو IDOR خادمية قابلة للتطبيق حاليًا لأن المشروع لا يملك خادم بيانات أو جلسات أو SQL. هذا لا يجعل التطبيق آمنًا؛ بل ينقل المخاطر إلى سرية وسلامة البيانات المحلية والتصدير.

### حرجة

لا توجد ثغرة حرجة مثبتة قابلة للاستغلال عن بعد من المصدر الحالي.

### عالية

| الخطر | الأثر | الدليل | المعالجة المطلوبة |
|---|---|---|---|
| غياب المصادقة والتفويض كليًا | كل من يصل للتطبيق/المتصفح يرى ويغير البيانات المالية وصور الحوالات | جميع المسارات عامة؛ لا auth/proxy/API | مصادقة موثوقة + authorization خادمي + أدوار |
| localStorage هو المصدر الوحيد بلا نسخة احتياطية | مسح بيانات الموقع/تغيير الجهاز أو origin يفقد كامل السجل | `invoice-storage.ts` | قاعدة بيانات ونسخ احتياطي/استعادة وتصدير آمن |
| صور الحوالات والبيانات المالية مخزنة نصيًا في المتصفح | أي JavaScript على origin أو مستخدم محلي يستطيع قراءتها؛ مساحة التخزين محدودة | `transferReceipt` داخل `Invoice` و`localStorage` | object storage خاص، URLs موقعة، CSP، وتقليل بيانات العميل |
| لا سجل تدقيق ولا حماية للحذف/التعديل | تزوير أو حذف غير قابل للإسناد أو الاسترجاع | `invoices-table.tsx` و`invoice-storage.ts` | audit log وsoft delete وصلاحيات ونسخ تاريخية |
| CSV Formula Injection | إدخال يبدأ بـ`=`, `+`, `-`, `@` في العميل/البائع/الملاحظات قد ينفذ كصيغة عند فتح CSV | `csv-export.ts:7-13` يقتبس فقط ولا يعطل الصيغ | تحييد الصيغ وتوثيق encoding واختبارات |

### متوسطة

| الخطر | الدليل/التفسير |
|---|---|
| لا حدود `Number.isFinite` لمعظم الأرقام | `validateForm` يقبل Infinity في السعر/التكلفة/الأبعاد/الرسوم؛ JSON يحوله إلى `null` فيفسد سلامة السجل (`invoice-form.tsx:139-181`) |
| التطبيع ليس schema validation | `normalizeInvoice` يقبل كائنات جزئية وأنواعًا غير متحققة من localStorage (`invoice-normalize.ts`) |
| JSON التالف يتحول بصمت إلى قائمة فارغة | قد يظن المستخدم أن البيانات اختفت ولا توجد آلية إصلاح/تصدير raw (`invoice-storage.ts:40-46`) |
| غياب security headers/CSP | `next.config.ts` فارغ؛ لا CSP أو frame-ancestors أو nosniff أو Referrer-Policy |
| لا قفل تنافسي أو استماع بين التبويبات | عمليتان قد تكتبان فوق بعضهما؛ pub/sub داخل الذاكرة فقط |
| أرقام الفواتير ليست قيدًا فريدًا مركزيًا | فحص Set محلي ثم suffix عشوائي (`invoice-number.ts`) |
| اعتماد build على Google Fonts | انقطاع الشبكة منع البناء الأول؛ reproducibility أقل |

### منخفضة

- لا rate limiting، لكنه غير قابل للتطبيق قبل وجود API؛ يجب إضافته مع الخادم.
- لا حماية clickjacking أو policy للـreferrer.
- لا مراقبة للأخطاء أو تنبيه أمني.
- لا تدوير أسرار، لكن لا أسرار أو env مستخدمة أصلًا.

### مراجعة فئات محددة

| الفئة | الحكم الحالي |
|---|---|
| XSS | React يهرب النصوص ولا يوجد `dangerouslySetInnerHTML`; لم تثبت XSS، لكن CSP غائبة |
| CSRF | غير منطبق بلا جلسات أو mutations خادمية |
| SQL injection | غير منطبق بلا SQL/API |
| Open redirects | لم توجد redirects مشتقة من مدخل مستخدم |
| IDOR | لا مورد خادمي؛ معرف edit مكشوف لكن البيانات محلية أصلًا |
| رفع الملفات | الصور تعاد معالجتها عبر Canvas إلى WebP؛ جيد، لكن التحقق عميل فقط والتخزين غير مناسب للإنتاج |
| Service role | غير موجود |
| أسرار داخل الكود | لم توجد مفاتيح أو أسرار في الملفات المفحوصة |

## 16. الأخطاء والنواقص

### أخطاء/عيوب مثبتة

1. **CSV Formula Injection:** `escapeCsvField` يعالج الفواصل والاقتباس والسطر الجديد فقط (`src/lib/utils/csv-export.ts:7-13`).
2. **قبول أرقام غير منتهية:** أغلب التحقق يستخدم `isNaN` والمقارنة ولا يستخدم `Number.isFinite` (`invoice-form.tsx:139-181`).
3. **عدم التزامن بين التبويبات:** لا listener لحدث `window.storage` (`invoice-storage-events.ts`).
4. **عنوان accessible فارغ في Modal التفاصيل:** `title=""` مع `aria-labelledby` (`invoice-details-modal.tsx:48-53`).
5. **اختبارات منطقية منفصلة عن المصدر الحقيقي:** السكربتان يكرران/يحاكيان المنطق.

### نواقص وظيفية

- تسجيل/دخول/خروج/استعادة/تحديث كلمة المرور: غير موجود.
- ملف شخصي وربطه بالحساب: غير موجود.
- تحقق بريد/جوال: غير موجود.
- حظر/تعطيل/حذف حساب: غير موجود.
- قاعدة بيانات ومزامنة ونسخ احتياطي: غير موجود.
- إدارة منتجات ومخزون وعملاء وبائعين: غير موجودة.
- دورة طلب/قبول/رفض: غير موجودة.
- دفع إلكتروني وفواتير ضريبية وVAT/refund: غير موجودة.
- إشعارات داخل التطبيق/بريد/SMS/WhatsApp: غير موجودة.
- حالات تسليم وإثبات تسليم: غير موجودة.
- PDF/طباعة فاتورة: غير موجود.
- إعدادات وإدارة مستخدمين وصلاحيات: غير موجودة.
- PWA/offline sync: غير موجود.
- بحث نصي وpagination وفرز الجدول: غير موجودة.
- استيراد/تصدير نسخة احتياطية JSON: غير موجود.
- monitoring/analytics/observability: غير موجودة.

### توثيق قديم أو متناقض

- `README.md` ما زال قالب Create Next App ولا يشرح FoamSales أو التخزين أو القيود.
- `docs/FOAMSALES_HANDOFF_REPORT.md:154-190` يعرض نموذج دفع قديم (`credit` داخل `PaymentMethod`) قبل أن تصححه الأقسام اللاحقة؛ النموذج الحالي الصحيح في `src/lib/types/invoice.ts`.
- خريطة الملفات والإحصائيات المبكرة في تقرير التسليم لا تشمل كل ملفات الإصدار الثاني أو كل بطاقات/فلاتر الوضع الحالي.

## 17. الديون التقنية

| الدين | الأثر | الأولوية |
|---|---|---|
| `invoice-form.tsx` بحجم 638 سطرًا | صعوبة الاختبار والصيانة وتداخل validation/state/UI | عالية |
| `sales-charts.tsx` يجمع البيانات ويرسمها في ملف واحد | صعوبة الاختبار والأداء مع بيانات كبيرة | متوسطة |
| تخزين قيم مشتقة مع مدخلاتها | احتمال عدم الاتساق بعد تغيّر معادلات الحساب | عالية |
| لا schema runtime مثل Zod | بيانات localStorage غير موثوقة قد تمر بأنواع خاطئة | عالية |
| لا repository/API abstraction غير متزامن | الانتقال لخادم سيحتاج تغيير hooks والتدفقات، رغم تمركز storage جزئيًا | متوسطة |
| الاختبارات تكرر المنطق | ثقة زائفة | عالية |
| README وتقرير التسليم غير متزامنين مع المصدر | onboarding وقرارات خاطئة | متوسطة |
| client-heavy architecture | bundle وعمل التقارير كله على جهاز العميل | متوسطة |
| لا pagination/virtualization | الجدول والرسوم لا تتوسع جيدًا | متوسطة |
| لا Error Boundaries أو observability | أعطال العميل غير مرصودة | متوسطة |
| أصول branding افتراضية | منتج غير مكتمل بصريًا | منخفضة |

لا استخدام لـ`any` صريح في مصدر `src`، ولا TODO/FIXME ظاهرة، ولا dead API routes مثبتة. توجد أنواع casts محدودة ومدخلات runtime غير موثقة كما سبق.

## 18. المزايا المكتملة

المقصود هنا مكتملة **ضمن نطاق التخزين المحلي أحادي المستخدم**:

- إنشاء فاتورة مع تحقق أساسي.
- تعديل وحذف وعرض التفاصيل.
- حساب subtotal/total/cost/net profit/margin.
- تسجيل paid/partial/deferred وحساب المدفوع والمتبقي.
- مواصفات الفلين والوزن الاختياري.
- ضغط إثبات الحوالة وعرضه.
- إحصائيات الرئيسية الحقيقية.
- فلاتر التقارير السبعة الحالية.
- 8 KPIs ورسوم المبيعات/الأرباح/السداد/البائعين.
- جدول الفواتير وإجراءات العرض/التعديل/الحذف.
- تصدير النتائج المفلترة CSV، مع فجوة الأمان المذكورة.
- قراءة بيانات legacy وتحويل `credit` منطقيًا.
- RTL وثيم عربي وToast وحالات فراغ.

## 19. المزايا الجزئية

| الميزة | ما الموجود | ما ينقص |
|---|---|---|
| التحصيل | حالة ومبالغ وملخص | دفعات، سجل، مواعيد، تذكير، تسوية، صلاحيات |
| التسليم | تاريخ | حالة وتنفيذ وإثبات وإلغاء |
| إدارة البائعين والعملاء | أسماء وفلاتر مشتقة | كيانات وصفحات وعلاقات ودمج وتحقق |
| أرقام الفواتير | توليد وتصادم محلي | sequence/unique constraint مركزي |
| رفع الملفات | صورة واحدة مضغوطة | تخزين خاص، scanning، lifecycle، سياسات وصول |
| الأخطاء | رسائل validation وquota وبعض empty states | error boundaries، recovery، logging |
| الاستجابة | classes responsive وتمرير جدول | اختبار أجهزة وتحسين كثافة الجدول/الرسوم |
| الاختبارات | سكربتان + lint/tsc/build | unit/integration/E2E/a11y حقيقية |
| الترحيل القديم | normalization عند القراءة | versioned migrations ورفض/إصلاح البيانات التالفة |

## 20. المزايا غير المتصلة أو الوهمية

- بطاقة معاينة الفاتورة في Hero ثابتة بقيم `FS-20260531-A1B2` و14,400 و2,160، لكنها تحمل نصًا واضحًا بأنها مثال (`hero-section.tsx`).
- بطاقات المزايا في الرئيسية محتوى تسويقي ثابت، وليست وحدات مستقلة.
- ملفات SVG الافتراضية في `public/` غير مستخدمة حسب البحث في المصدر.
- لا توجد أزرار واضحة تؤدي إلى ميزات backend وهمية؛ معظم الأزرار الحالية متصلة بتدفق محلي فعلي.
- لا توجد صفحة إدارة شكلية أو دفع شكلي؛ الميزات نفسها غير موجودة.

## 21. المطلوب قبل الإطلاق

### شروط مانعة للإطلاق التجاري

1. تصميم backend وقاعدة بيانات مع migrations وقيود وفهارس ونسخ احتياطي.
2. مصادقة وجلسات آمنة وأدوار واضحة (مالك/مدير/مبيعات/محاسب أو ما يقرره العمل).
3. فرض الصلاحيات على الخادم لكل قراءة وmutation، وليس إخفاء الواجهة فقط.
4. نقل صور الحوالات إلى object storage خاص بسياسات وصول وروابط موقعة.
5. مسار ترحيل آمن من localStorage مع export/import واستعادة.
6. معالجة CSV Formula Injection والتحقق runtime من كل المدخلات والأرقام المنتهية.
7. audit log وsoft delete وإدارة استرجاع الفواتير.
8. تعريف المتطلبات المحاسبية والضريبية السعودية إن كانت الفاتورة مستندًا رسميًا.
9. اختبارات unit/integration/E2E للرحلات الحساسة، بما فيها الصور والـquota والبيانات القديمة.
10. security headers/CSP وrate limiting وobservability عند إضافة الخادم.

### شروط تشغيلية وتجربة مستخدم

- استضافة وdomains وبيئات dev/staging/prod موثقة.
- self-host خط Tajawal أو ضمان build network/caching موثوق.
- صفحات error/loading/not-found مخصصة.
- تحسين الوصول في Modal والتنقل بلوحة المفاتيح.
- branding وأيقونات نهائية.
- مراقبة الأخطاء، logging، metrics، وسياسة احتفاظ بالبيانات.
- اختبار جوال ومتصفحات وطباعة/CSV ببرامج الجداول الفعلية.

## 22. ترتيب الأولويات المقترح

| الترتيب | العمل | السبب |
|---:|---|---|
| 1 | تثبيت المتطلبات والأدوار ونموذج البيانات | يمنع بناء خادم بصلاحيات أو علاقات خاطئة |
| 2 | قاعدة بيانات + API/DAL + migrations ونسخ احتياطي | يحل الخطر الأكبر: الاعتماد على متصفح واحد |
| 3 | مصادقة وتفويض خادمي وaudit log | يحمي البيانات والعمليات المالية |
| 4 | تخزين خاص لصور الحوالات وسياسات وصول | يعزل الملفات الحساسة عن localStorage |
| 5 | ترحيل البيانات المحلية وsoft delete/recovery | يحافظ على بيانات المستخدمين الحاليين |
| 6 | validation runtime وCSV hardening وsecurity headers | يغلق عيوب السلامة والتصدير المثبتة |
| 7 | اختبارات حقيقية تستورد كود الإنتاج + E2E | تمنح ثقة قبل الانتقال والإطلاق |
| 8 | تفكيك InvoiceForm والرسوم وطبقة data access | يحسن الصيانة والتوسع |
| 9 | استكمال التحصيل والتسليم والعملاء/البائعين حسب العمل | يحول السجل إلى نظام تشغيل فعلي |
| 10 | PWA/إشعارات/تحسينات بصرية | تأتي بعد سلامة البيانات والعمليات الأساسية |

## 23. أهم الملفات في المشروع

| الملف | سبب الأهمية |
|---|---|
| `src/lib/types/invoice.ts` | العقد المركزي لكل بيانات الفاتورة |
| `src/components/sales/invoice-form.tsx` | أكبر تدفق: state والتحقق والحساب والحفظ |
| `src/lib/storage/invoice-storage.ts` | مصدر البيانات وCRUD ومعالجة quota/cache |
| `src/lib/utils/invoice-normalize.ts` | التوافق مع البيانات القديمة |
| `src/lib/utils/invoice-calculations.ts` | المعادلات المالية والتحصيل |
| `src/lib/utils/invoice-filters.ts` | الفلاتر وKPIs وملخص التحصيل |
| `src/app/reports/page.tsx` | تنسيق لوحة التقارير |
| `src/components/reports/invoices-table.tsx` | عرض السجل والعمليات الحساسة |
| `src/components/reports/sales-charts.tsx` | تحليلات الرسوم والتجميع |
| `src/lib/utils/csv-export.ts` | تصدير البيانات ومنطقة خطر formula injection |
| `src/lib/utils/image-compression.ts` | معالجة ملفات الحوالة |
| `src/lib/hooks/use-invoices.ts` | جسر React مع التخزين المحلي |
| `src/app/layout.tsx` | metadata وRTL والخط والرأس وToast |
| `src/app/globals.css` | design tokens والثيم |
| `package.json` | الإصدارات والأوامر وحدود الاختبار |
| `docs/FOAMSALES_HANDOFF_REPORT.md` | تاريخ التنفيذ، لكنه يحتاج تنقية من المعلومات القديمة |

## 24. الخلاصة النهائية

FoamSales تطبيق Frontend منظم نسبيًا وينفذ نواة مفيدة لإدارة فواتير الفلين على متصفح واحد. المصدر الحالي يبني بنجاح، وTypeScript وESLint نظيفان، والواجهة تربط الإنشاء والتعديل والحذف والتقارير بمصدر بيانات محلي حقيقي. أفضل ما فيه هو وضوح نطاق Invoice، تمركز منطق الحساب، دعم RTL، وإعادة استخدام النموذج والمكونات.

لكن سلامة المنتج لا تساوي سلامة البناء. غياب الخادم والحسابات والصلاحيات والنسخ الاحتياطي يجعل بيانات الفواتير وصور الحوالات عرضة للفقد والوصول المحلي غير المنضبط، ويمنع التوسع أو التدقيق. كما أن الاختبارات الحالية لا تستورد منطق الإنتاج، وفحص الاعتماديات الحي لم يكتمل بسبب شهادة البيئة، والفحص البصري الحي لم يُنفذ. لذلك لا يُنصح بإطلاقه على بيانات مالية فعلية قبل إغلاق البنود المانعة أعلاه.

### التقييم من 100

| الجانب | الدرجة | المبرر المختصر |
|---|---:|---|
| اكتمال المنتج | 58 | نواة الفواتير والتقارير جيدة، ومعظم نظام الأعمال غير موجود |
| جودة الكود | 74 | strict/lint/build ناجحة وتمركز جيد، مع ملفات كبيرة واختبارات مكررة |
| الأمان | 28 | لا auth/authorization/خادم/سياسات؛ بيانات مالية وصور في localStorage |
| تجربة المستخدم | 70 | RTL وتصميم وحالات جيدة، مع نواقص accessibility واختبار بصري |
| الجاهزية للإطلاق | **32** | يصلح كتجربة محلية، لا كنظام إنتاج مالي |
| قابلية التوسع | 22 | كل البيانات والحسابات على عميل واحد بلا DB أو pagination |
| سهولة الصيانة | 66 | هيكل مجلدات جيد وأنواع واضحة، لكن InvoiceForm كبير والتوثيق متضارب |

### الأعداد النهائية المكتشفة

- صفحات/مسارات المنتج: **4**.
- مسارات API: **0**.
- جداول قاعدة البيانات: **0**، مع مجموعة localStorage منطقية واحدة.
- الأدوار الرسمية: **0**، مع نمط وصول فعلي واحد غير موثق وغير مقيّد.
- Client Components: **19**.
- ملفات source تحت `src`: **42**.

### حالة Git وقت التدقيق

- الفرع: `master`.
- آخر commit والوحيد الظاهر: `1018a2b` بتاريخ 2026-05-31 بعنوان `Initial commit from Create Next App`.
- توجد تغييرات محلية غير محفوظة: `package-lock.json` و`package.json` و`src/app/globals.css` و`src/app/layout.tsx` و`src/app/page.tsx` معدلة، ومعظم `docs/` و`scripts/` و`src/app/reports/` و`src/app/sales/` و`src/components/` و`src/lib/` غير متتبعة.
- لم ينفذ التدقيق commit أو push أو checkout أو reset أو clean أو migration، ولم يغير قاعدة بيانات.
