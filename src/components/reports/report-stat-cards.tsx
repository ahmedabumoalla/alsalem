"use client";
import { Card } from "@/components/ui/card";import type { InvoiceStats } from "@/lib/utils/invoice-filters";import { formatCurrency,formatNumber,formatPercent } from "@/lib/utils/format";
export function ReportStatCards({stats}:{stats:InvoiceStats}){const cards=[{label:"إجمالي المبيعات",value:formatCurrency(stats.totalSales)},{label:"إجمالي التكلفة",value:formatCurrency(stats.totalCost)},{label:"صافي الربح",value:formatCurrency(stats.totalProfit)},{label:"عدد الفواتير",value:formatNumber(stats.invoiceCount,0)},{label:"عدد الأصناف",value:formatNumber(stats.itemCount,0)},{label:"إجمالي الكمية",value:formatNumber(stats.totalQuantity,0)},{label:"متوسط الفاتورة",value:formatCurrency(stats.averageInvoiceValue)},{label:"هامش الربح",value:formatPercent(stats.averageProfitMargin)}];return <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{cards.map((card)=><Card key={card.label} className="p-5"><p className="text-xs text-muted">{card.label}</p><p className="mt-2 text-2xl font-bold text-primary">{card.value}</p></Card>)}</div>}
